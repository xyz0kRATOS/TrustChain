{
  "name": "trustchain-subgraph",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create trustchain-subgraph --node http://127.0.0.1:8020",
    "deploy-local": "graph deploy trustchain-subgraph --node http://127.0.0.1:8020 --ipfs http://127.0.0.1:5001"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.83.0"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.36.0"
  }
}
